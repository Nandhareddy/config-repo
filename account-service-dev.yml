server:
  port: 9000

spring:
  application:
    name: AccountService
  datasource:
    url: jdbc:mysql://localhost:3306/accountsdb
    username: root
    password: Zack@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: ALWAYS
      
 management:
  endpoints:
    web:
      exposure:
        include: health,info     